.claude
settings.local.json
.DS_Store
node_modules/

# Foundry artifacts (anywhere except the compiled Noir circuit outputs in circuits/target/)
contracts/out/
contracts/cache/
app/data/
app/dist/
app/persist/
app/src/generated.ts
.anvil.pid
.server.pid
.server.log
CLAUDE.md
TODO.md

# reference image thumbnails (regenerable, used for sub-agent visual review)
.thumbs/

# private working materials — kept locally, not in repo
references/
context/
previews/*
# ...except the on-chain preview JPGs, which are uploaded to the
# metadata contract and should travel with the source.
!previews/onchain/
